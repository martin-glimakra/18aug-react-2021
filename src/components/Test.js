export default function Test(props) {
    // Datan som visas i den här komponenten kommer ursprungligen från testdata/data-sortera.json

    return (
        <div >
            {props.data.rubrik}
       </div>
    )
}
